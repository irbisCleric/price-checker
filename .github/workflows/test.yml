name: Test Cron

on:
    schedule:
        - cron: '*/5 * * * *' # Запуск кожні 5 хвилин
    workflow_dispatch:

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: Test Step
              run: echo "Hello, World! $(date -u)"
